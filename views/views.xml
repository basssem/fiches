<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="fiches.list">
      <field name="name">Fiches list</field>
      <field name="model">fiches.fiches</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="fiches.action_window">
      <field name="name">Fiches window</field>
      <field name="res_model">fiches.fiches</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="fiches.action_server">
      <field name="name">Fiches server</field>
      <field name="model_id" ref="model_fiches_fiches"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Fiches" id="fiches.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="fiches.menu_1" parent="fiches.menu_root"/>
    <menuitem name="Menu 2" id="fiches.menu_2" parent="fiches.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="fiches.menu_1_list" parent="fiches.menu_1"
              action="fiches.action_window"/>
    <menuitem name="Server to list" id="fiches" parent="fiches.menu_2"
              action="fiches.action_server"/>
    -->
  </data>
</odoo>